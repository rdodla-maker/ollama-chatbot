# Optional MongoDB configuration (legacy). Move these to .env if you intend
# to enable MongoDB-based storage. The application defaults to SQLite + SQLAlchemy.
MONGODB_URL=mongodb+srv://user:password@cluster0.example.mongodb.net/?appName=Cluster0
DATABASE_NAME=career_copilot

# Ollama / third-party configs follow in your real .env
OLLAMA_URL=http://localhost:11434
N8N_WEBHOOK_URL=
GOOGLE_SHEETS_ID=
GOOGLE_SERVICE_ACCOUNT_PATH=
